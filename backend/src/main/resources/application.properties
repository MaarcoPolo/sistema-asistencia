spring.application.name=asistencia-backend
# Conexión a la Base de Datos PostgreSQL con el nuevo usuario
spring.datasource.url=jdbc:postgresql://localhost:5432/asistencia_db
spring.datasource.username=asistencia_user
spring.datasource.password=vnlaGfYu9CYJKlh2

# Configuración de Hibernate (JPA)
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
# Indica a JPA en qué esquema debe buscar las tablas
spring.jpa.properties.hibernate.default_schema=asistencia

# Configuración de Flyway
# Le decimos a Flyway en qué esquema debe trabajar
spring.flyway.schemas=asistencia
app.jwt.secret=esta-es-una-clave-secreta-MUY-larga-y-dificil-de-adivinar-para-mi-sistema
app.jwt.expiration-ms=3600000
# Activa el log detallado para el módulo de seguridad
logging.level.org.springframework.security=DEBUG